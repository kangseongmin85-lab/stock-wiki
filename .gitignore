# 환경 변수 (API 키 포함) — 절대 커밋 금지
.env
*.env
# config.json (API 키 포함) — 절대 커밋 금지
config.json
# KIS 토큰 캐시 (있으면 안 되지만 만일 대비)
kis_token.json

# Python 캐시
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# 로컬 가상환경
venv/
.venv/
env/

# 임시 파일
*.log
*.tmp
.DS_Store
Thumbs.db

# 뉴스 seen_ids (로컬 캐시 — GitHub Actions에서 별도 관리)
wiki/news/seen_ids.json

# Obsidian 로컬 설정
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
".env"

# 아카이브/로그 — 옛 백업·로그 모음. setup_github_secrets.ps1 등 비밀키 하드코딩 파일 포함 → 절대 커밋 금지
_archive/

# 명령 오타로 생긴 0바이트 쓰레기 파일
/git
/main

# 뉴스봇 설정 가이드 — 예제 블록에 실제 NOTION/NAVER 토큰·chat_id 등 하드코딩 → 절대 커밋 금지
_뉴스봇_가이드/
